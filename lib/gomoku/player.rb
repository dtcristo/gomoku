module Gomoku
  # Defines common functionality for all players
  class Player
    def initialize(window, board, color)
      @window = window
      @board = board
      @color = color
    end

    def update
    end

    def draw
    end
  end
end
